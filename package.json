{
  "name": "dicom-ml-forward",
  "version": "0.4.0",
  "license": "MIT",
  "description": "Sample project as proof of concept to take dicoms and pass them through an ml forward",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ManaseD/dicom-ml-forward"
  },
  "bugs": {},
  "scripts": {
    "db:reset": "docker-compose -f docker-compose.yml run --rm api node scripts db/reset",
    "psql": "docker-compose -f docker-compose.yml exec postgres psql -U dcm dcm"
  },
  "dependencies": {
    "node-base64-image": "^1.0.6",
    "request": "^2.88.0",
    "request-promise": "^4.2.4"
  }
}
